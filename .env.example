# Copy to .env.local and fill in. Never commit .env.local.
#
# Used server-side only, inside api/analyze.ts. This key is never sent to the
# browser — putting it in a VITE_ prefixed variable would bundle it into the
# client and expose it to anyone who opens devtools.
ANTHROPIC_API_KEY=
